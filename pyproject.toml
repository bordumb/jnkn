[project]
name = "jnkn-monorepo"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = []

# CORRECT: 'members' belongs under [tool.uv.workspace]
[tool.uv.workspace]
members = [
    "packages/jnkn-core",
    "packages/jnkn-ai"
]